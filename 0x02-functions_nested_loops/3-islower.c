#include "main.h"

/**
 * int _islower(int c) - checks if a character is lower case.
 * @c: the character to check
 * return: 0.
 */

int _islower(int c)
{
	if (c >= 'a' && c = 'z')
		return (1);
	else
		return (0);
	_putchar('\n');
}
